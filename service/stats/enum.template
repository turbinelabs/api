/*
Copyright 2018 Turbine Labs, Inc.

Licensed under the Apache License, Version 2.0 (the "License");
you may not use this file except in compliance with the License.
You may obtain a copy of the License at

    http://www.apache.org/licenses/LICENSE-2.0

Unless required by applicable law or agreed to in writing, software
distributed under the License is distributed on an "AS IS" BASIS,
WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
See the License for the specific language governing permissions and
limitations under the License.
*/

// This file was automatically generated by {{ .Source }} from {{ .Template }}.
// Any changes will be lost if this file is regenerated.

package {{ .type.Package }}

import (
	"encoding/json"
	"fmt"
)

// {{ .type.Capitalize }} is an enumeration.
type {{.type.Capitalize}} int

// Defined values of {{ .type.Capitalize }}.
const (
{{- range $idx, $elem := .values }}
	{{$elem.Capitalize}} {{- if eq $idx 0 }} {{$.type.Capitalize}} = iota{{end}}
{{- end}}
)

var _dummy = {{.type.Capitalize}}(0)
var _ json.Marshaler = &_dummy
var _ json.Unmarshaler = &_dummy

const (
{{- range .values }}
	str{{.Capitalize}} = "{{.SnakeCase}}"
{{- end}}
)

var {{.type.VarName}}Names = [...]string{
{{- range .values}}
	str{{.Capitalize}},
{{- end}}
}

{{if eq .values.Last.Capitalize "Unknown" -}}
const min{{.type.Capitalize }} = {{.type.Capitalize}}(0)

var max{{.type.Capitalize}} = {{.type.Capitalize}}(len({{.type.VarName}}Names) - 2)
{{- else -}}
{{- if eq .values.First.Capitalize "Unknown" -}}
const min{{.type.Capitalize}} = {{.type.Capitalize}}(1)
{{else -}}
const min{{.type.Capitalize}} = {{.type.Capitalize}}(0)
{{end}}
var max{{.type.Capitalize}} = {{.type.Capitalize}}(len({{.type.VarName}}Names) - 1)
{{end}}
// IsValid returns a boolean indicating whether the given {{.type.Capitalize}} is defined
// (valid) or not.
func IsValid(i {{.type.Capitalize}}) bool {
	return i >= min{{.type.Capitalize}} && i <= max{{.type.Capitalize}}
}

// FromName converts a {{.type.Capitalize}} name into a {{.type.Capitalize}}.
// Returns Unknown if the name is not known. Names are case sensitive.
func FromName(s string) {{.type.Capitalize}} {
	for idx, name := range {{.type.VarName}}Names {
		candidate := {{.type.Capitalize}}(idx)
		if IsValid(candidate) && name == s {
			return candidate
		}
	}

	return Unknown
}

// ForEach invokes the given function for each valid {{.type.Capitalize}}.
func ForEach(f func({{.type.Capitalize}})) {
	for i := int(min{{.type.Capitalize}}); i <= int(max{{.type.Capitalize}}); i++ {
		tg := {{.type.Capitalize}}(i)
		f(tg)
	}
}

// String return this {{.type.Capitalize}}'s string representation.
func (i {{.type.Capitalize}}) String() string {
	if !IsValid(i) {
		return fmt.Sprintf("unknown(%d)", i)
	}
	return {{.type.VarName}}Names[i]
}

// MarshalJSON converts this {{.type.Capitalize}} to a quoted JSON string. Returns an
// error if the {{.type.Capitalize}} is nil or invalid.
func (i *{{.type.Capitalize}}) MarshalJSON() ([]byte, error) {
	if i == nil {
		return nil, fmt.Errorf("cannot marshal unknown {{.type.Capitalize}} (nil)")
	}

	qt := *i
	if !IsValid(qt) {
		return nil, fmt.Errorf("cannot marshal unknown {{.type.Capitalize}} (%d)", qt)
	}

	name := {{.type.VarName}}Names[qt]
	b := make([]byte, 0, len(name)+2)
	b = append(b, '"')
	b = append(b, name...)
	return append(b, '"'), nil
}

// UnmarshalJSON converts a quoted JSON string into a {{.type.Capitalize}}. Returns an
// error if the receiver is nil, the JSON is not a quoted string, or if the
// string does not represent a valid {{.type.Capitalize}}. Otherwise, the receiver's value
// is set to the {{.type.Capitalize}} represented by the string.
func (i *{{.type.Capitalize}}) UnmarshalJSON(bytes []byte) error {
	if i == nil {
		return fmt.Errorf("cannot unmarshal into nil {{.type.Capitalize}}")
	}

	length := len(bytes)
	if length <= 2 || bytes[0] != '"' || bytes[length-1] != '"' {
		return fmt.Errorf("cannot unmarshal invalid JSON: %q", string(bytes))
	}

	unmarshalName := string(bytes[1 : length-1])

	qt := FromName(unmarshalName)
	if qt == Unknown {
		return fmt.Errorf(
			"cannot unmarshal unknown {{.type.Capitalize}} %q",
			unmarshalName,
		)
	}

	*i = qt
	return nil
}

// UnmarshalForm converts a string into a {{.type.Capitalize}}. Returns an error if the
// receiver is nil or if the string does not represent a valid {{.type.Capitalize}}.
// Otherwise, the receiver's value is set to the {{.type.Capitalize}} represented by the
// string.
func (i *{{.type.Capitalize}}) UnmarshalForm(value string) error {
	if i == nil {
		return fmt.Errorf("cannot unmarshal into nil {{.type.Capitalize}}")
	}

	qt := FromName(value)
	if qt == Unknown {
		return fmt.Errorf("cannot unmarshal unknown {{.type.Capitalize}} %q", value)
	}

	*i = qt
	return nil
}
