/*
Copyright 2018 Turbine Labs, Inc.

Licensed under the Apache License, Version 2.0 (the "License");
you may not use this file except in compliance with the License.
You may obtain a copy of the License at

    http://www.apache.org/licenses/LICENSE-2.0

Unless required by applicable law or agreed to in writing, software
distributed under the License is distributed on an "AS IS" BASIS,
WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
See the License for the specific language governing permissions and
limitations under the License.
*/

// This file was automatically generated by {{.Source}} from {{.Template}}.
// Any changes will be lost if this file is regenerated.

package {{.type.Package}}

import (
	"encoding/json"
	"fmt"
	"testing"

	"github.com/turbinelabs/test/assert"
)

{{$testVal := .values.At 1 -}}

type testStruct struct {
	{{.type.Capitalize}} {{.type.Capitalize}} `json:"{{.type.SnakeCase}}"`
}

func assertHasAllValues(t testing.TB, i interface{}) {
	mappedValues := map[{{.type.Capitalize}}]struct{}{}

	switch m := i.(type) {
	case map[{{.type.Capitalize}}]string:
		for qt := range m {
			mappedValues[qt] = struct{}{}
		}
	case map[string]{{.type.Capitalize}}:
		for _, qt := range m {
			mappedValues[qt] = struct{}{}
		}
	default:
		t.Fatalf("cannot validate type %T", i)
		return
	}

	ForEach(func(v {{.type.Capitalize}}) {
		assert.Group(
                	fmt.Sprintf("{{.type.Capitalize}} %s", v.String()),
                        t,
                        func(g *assert.G) {
				_, ok := mappedValues[v]
				assert.True(g, ok)
			},
		)
	})
}

func Test{{.type.Capitalize}}String(t *testing.T) {
	assert.Equal(
		t,
		{{$testVal.Capitalize}}.String(),
		"{{$testVal.SnakeCase}}",
	)
	assert.MatchesRegex(t, Unknown.String(), `unknown\([0-9]+\)`)
	assert.Equal(t, {{.type.Capitalize}}(100).String(), "unknown(100)")
}

func TestIsValid(t *testing.T) {
	invalid := []{{.type.Capitalize}}{
		{{.type.Capitalize}}(min{{.type.Capitalize}} - 1),
		{{.type.Capitalize}}(max{{.type.Capitalize}} + 1),
	}

	for _, qt := range invalid {
		assert.False(t, IsValid(qt))
	}

	ForEach(func(qt {{.type.Capitalize}}) {
		assert.True(t, IsValid(qt))
	})
}

func TestFromName(t *testing.T) {
	validValues := map[{{.type.Capitalize}}]string{
{{- range .values -}}
{{- if ne .Capitalize "Unknown"}}
		{{.Capitalize}}: str{{.Capitalize}},
{{- end -}}
{{- end}}
	}
	assertHasAllValues(t, validValues)

	for expectedQt, name := range validValues {
		qt := FromName(name)
		assert.Equal(t, qt, expectedQt)
	}

	invalidValues := []string{"bob", "unknown", "1"}

	for _, name := range invalidValues {
		qt := FromName(name)
		assert.Equal(t, qt, Unknown)
	}
}

func Test{{.type.Capitalize}}MarshalJSON(t *testing.T) {
	vals := map[{{.type.Capitalize}}]string{
{{- range .values -}}
{{- if ne .Capitalize "Unknown"}}
		{{.Capitalize}}: str{{.Capitalize}},
{{- end -}}
{{- end}}
	}
	assertHasAllValues(t, vals)

	for v, name := range vals {
		bytes, err := v.MarshalJSON()
		assert.Nil(t, err)
		expected := []byte(fmt.Sprintf(`"%s"`, name))
		assert.DeepEqual(t, bytes, expected)
	}
}

func Test{{.type.Capitalize}}MarshalJSONUnknown(t *testing.T) {
	unknownValues := []{{.type.Capitalize}}{
		Unknown,
		{{.type.Capitalize}}(max{{.type.Capitalize}} + 1),
	}

	for _, unknown{{.type.Capitalize}} := range unknownValues {
		bytes, err := unknown{{.type.Capitalize}}.MarshalJSON()
		assert.Nil(t, bytes)
		assert.ErrorContains(t, err, "cannot marshal unknown {{.type.Capitalize}}")
	}
}

func Test{{.type.Capitalize}}MarshalJSONNil(t *testing.T) {
	var v *{{.type.Capitalize}}

	bytes, err := v.MarshalJSON()
	assert.ErrorContains(t, err, "cannot marshal unknown")
	assert.Nil(t, bytes)
}

func Test{{.type.Capitalize}}UnmarshalJSON(t *testing.T) {
	quoted := func(s string) string {
		return fmt.Sprintf(`"%s"`, s)
	}

	vals := map[string]{{.type.Capitalize}}{
{{- range .values -}}
{{- if ne .Capitalize "Unknown"}}
		quoted(str{{.Capitalize}}): {{.Capitalize}},
{{- end -}}
{{- end}}
	}
	assertHasAllValues(t, vals)

	for data, expected := range vals {
		var v {{.type.Capitalize}}

		err := v.UnmarshalJSON([]byte(data))
		assert.Nil(t, err)
		assert.Equal(t, v, expected)
	}
}

func Test{{.type.Capitalize}}UnmarshalJSONUnknown(t *testing.T) {
	unknownVals := []string{`"unknown"`, `"nope"`}

	for _, unknownName := range unknownVals {
		var v {{.type.Capitalize}}

		err := v.UnmarshalJSON([]byte(unknownName))
		assert.ErrorContains(t, err, "cannot unmarshal unknown")
	}
}

func Test{{.type.Capitalize}}UnmarshalJSONNil(t *testing.T) {
	var v *{{.type.Capitalize}}

	err := v.UnmarshalJSON([]byte(`"{{$testVal.SnakeCase}}"`))
	assert.ErrorContains(t, err, "cannot unmarshal into nil {{.type.Capitalize}}")
}

func Test{{.type.Capitalize}}UnmarshalJSONInvalid(t *testing.T) {
	invalidNames := []string{``, `"`, `x`, `xx`, `"x`, `x"`, `'something'`}

	for _, invalidName := range invalidNames {
		var v {{.type.Capitalize}}

		err := v.UnmarshalJSON([]byte(invalidName))
		assert.ErrorContains(t, err, "cannot unmarshal invalid JSON")
	}
}

func Test{{.type.Capitalize}}UnmarshalForm(t *testing.T) {
	vals := map[string]{{.type.Capitalize}}{
{{- range .values -}}
{{- if ne .Capitalize "Unknown"}}
		str{{.Capitalize}}: {{.Capitalize}},
{{- end -}}
{{- end}}
	}
	assertHasAllValues(t, vals)

	for data, expected := range vals {
		var v {{.type.Capitalize}}

		err := v.UnmarshalForm(data)
		assert.Nil(t, err)
		assert.Equal(t, v, expected)
	}
}

func Test{{.type.Capitalize}}UnmarshalFormUnknown(t *testing.T) {
	unknownNames := []string{`unknown`, `nope`}

	for _, unknownName := range unknownNames {
		var v {{.type.Capitalize}}

		err := v.UnmarshalForm(unknownName)
		assert.ErrorContains(t, err, "cannot unmarshal unknown {{.type.Capitalize}}")
	}
}

func Test{{.type.Capitalize}}UnmarshalFormNil(t *testing.T) {
	var v *{{.type.Capitalize}}

	err := v.UnmarshalForm(`{{$testVal.SnakeCase}}`)
	assert.ErrorContains(t, err, "cannot unmarshal into nil {{.type.Capitalize}}")
}

func Test{{.type.Capitalize}}RoundTripStruct(t *testing.T) {
	expected := testStruct{ {{- $testVal.Capitalize -}} }

	bytes, err := json.Marshal(&expected)
	assert.Nil(t, err)
	assert.NonNil(t, bytes)
	assert.Equal(
		t,
		 string(bytes),
		`{"{{.type.SnakeCase}}":"{{$testVal.SnakeCase}}"}`,
	)

	var ts testStruct
	err = json.Unmarshal(bytes, &ts)
	assert.Nil(t, err)
	assert.Equal(t, ts, expected)
}
